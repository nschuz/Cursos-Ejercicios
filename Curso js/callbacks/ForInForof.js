//For of puede aplicarse son¿bre un objeto iterable cadena , map , arreglo . 

let arreglo = [2,3,4,4];
for (const iterator of arreglo) {
    console.log(numero);
}

//for in itera sobre las propiedad en vez de los valores 

let usaurio ={
    nombre : 'chuz',
    edad: 24
}

for (const key in usaurio) {
    console.log(propiedad);


}//muestra las propiedad 
//__protop__ no es interable porque es como oculta